<% provide(:title, @task.user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        
        <%= @task.user.name %>
        <%= @task.content %>
        <%= @task.user_id %>
        <%= @task.bet_user_id %>
        <%= @task.deadline_at %>
        <%= @task.amount_bet %>

      </h1>
    </section>
  </aside>
</div>